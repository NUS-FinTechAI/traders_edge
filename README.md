# traders_edge
By ming yuan and ha thu
trade

## Running locally - Basics
`docker compose up` from the root directory will spin up all containers. By default, frontend can be accessed at http://localhost:5173/.
